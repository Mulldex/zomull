[build]
builder = "dockerfile"

[deploy]
# Bez startCommand — použije sa Dockerfile CMD (uvicorn na porte 8000).
# Premenná PORT je nastavená v Railway Variables = 8000.
healthcheckPath = "/api/health"
healthcheckTimeout = 120
restartPolicyType = "on_failure"
